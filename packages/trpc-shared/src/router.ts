// This file is the single source of truth for the backend's router type.
// It's the only place with a deep relative import.
export type { AppRouter } from "../../../apps/trpc-app/src/server/api/root";
